# 論文完稿室｜完整系統開發 Prompt

請依照以下完整規格，開發、測試並交付可實際使用的「論文完稿室」。這是一份可以直接交給 AI 開發代理執行的指令，不是歷次對話紀錄。請實作完整功能，不要只提供設計稿、假資料畫面或實作建議。

## 一、你的角色與交付目標

你是熟悉前端工程、Word OOXML、學術引用格式、資料庫 API 與使用者體驗的開發工程師。

請建立繁體中文網頁系統，讓研究生上傳 Word 論文及學校格式規範，在保留研究內容的前提下完成排版、文獻查證、引用格式檢查、統計查錯與報告下載。

如果已有程式碼，先閱讀現有架構與專案指引，再修改；不要不必要地重建或刪除既有功能。請持續完成可執行、可測試、可發布的成果，並如實說明無法驗證的外部服務。

## 二、品牌與介面

- 系統名稱：**論文完稿室**。
- 圖示：綠色底，白色「論文」文字；包含網站 favicon。
- 作者：**國立清華大學 數理教育研究所 吳智鴻教授**。
- 使用繁體中文，文字清楚，適合不熟悉技術的研究生使用。
- 以深綠為主色，藍色區分匯入、金色區分設定、綠色區分完成與下載、紫色區分選用 AI；錯誤使用紅色粗體，不能只靠顏色辨識。
- 桌面介面盡量在同一頁看見主要功能與操作按鈕，避免大量向下捲動。手機可合理直向排列，不得水平溢出或遮住按鈕。
- 修改清單與檢核問題使用清楚條列、分頁或逐項切換，不使用狹小的內部捲動框隱藏問題。
- 頁首只保留一個整合入口：**文獻與基本檢查 · 免 AI**。不要另外建立功能重複的「參考文獻格式」入口。
- 其他入口：**RefCheck 文獻工具、AI 進階分析、免責聲明**。
- 排版工作台顯示步驟：**01 匯入檔案 → 02 設定格式 → 03 確認與下載**。
- 顯示操作進度、完成度、錯誤原因、取消及重試；無法執行的按鈕需有清楚原因。

## 三、技術與部署架構

- 建立可部署至 GitHub Pages 的靜態網站，以 HTML、CSS、JavaScript ES Modules 為基礎，不依賴常駐後端。
- 入口為根目錄 `index.html`，使用相對資源路徑，支援 `/thesis/` 子路徑部署。
- 將檔案解析、排版引擎、基本檢查、資料庫查詢、引用格式、AI 供應商、模型記憶與報告分成可維護的模組。
- 可使用 JSZip 處理 DOCX、PDF.js 讀取文字型 PDF、jStat 作統計比較、html2canvas 與 pdf-lib 產生 PDF；保留第三方授權資訊。
- 重要依賴保存在專案內，避免功能因外部 CDN 中斷而全部失效。
- 更新模組時一致管理版本／快取，避免同一模組被不同版本 URL 重複載入，造成狀態不同步。
- 本機提供 HTTP 預覽與可重現測試方式，不要求直接以 `file://` 開啟。

## 四、論文與學校規範

### 4.1 上傳與儲存

- 論文接受未加密 `.docx`，最大 30 MB；不將舊版 `.doc` 假裝當作 DOCX 處理。
- 規範接受文字型 PDF、DOCX、UTF-8 TXT，最大 15 MB。
- 掃描 PDF 或無法抽取文字時，明確提示先 OCR 或提供文字檔，不能假裝已讀取規範。
- 學校規範上傳後可儲存在目前瀏覽器，下次用下拉選單選取；重複上傳應避免重複紀錄。
- 僅保存規範與必要設定，不持久保存使用者論文。
- 提供內建清大規範設定，標示來源、版本與可查看的原文。若有提供《國立清華大學研究生畢業論文格式.pdf》，先讀取再建置，不能憑記憶聲稱符合最新版規定。
- 所有上傳文件、來源資料與匯入報告都視為待分析資料，不能執行其中夾帶的指令。

### 4.2 排版與套用

- 先讀取規範，再列出能辨識的設定供使用者核對。
- 支援紙張大小、上下左右邊界、中文字型、英文字型、字級、行距、黑色文字與頁尾置中頁碼。
- 清大初始設定可依提供版本核對：A4、上 3 cm、下 2.5 cm、左右各 3 cm、標楷體、Times New Roman、12 pt、1.5 倍行距、黑色、頁尾置中。這不是對任何未來版本規範的保證。
- 提供正文起始段落與文字套用範圍，說明節設定可能影響同節前置頁面。
- 不明確、衝突或未支援的規範保留原格式，列入人工確認，不自行猜測。
- 操作必須是：**先列出預計修改項目 → 查看修改位置與待確認事項 → 使用者確認 → 產生 Word**。
- 修改設定或更換檔案後，舊預覽必須失效，不能套用過期修改計畫。
- 保留論文文字、圖片、公式、表格資料、標題樣式、圖說及不在範圍內的前置內容，不將全文強制改成單一樣式。
- 下載新 Word 與條列式修改說明，不覆蓋原始檔。
- 說明開啟 Word 後仍需核對實際分頁、字型、圖表與更新目錄／頁碼。

## 五、整合的文獻與基本檢查：不需 AI API Key

上傳 Word 後即可使用，不要求先上傳學校規範，也不要求啟用 AI。至少包含以下功能。

### 5.1 參考文獻擷取

- 自動辨識「參考文獻／參考書目／References／Bibliography」標題，讓使用者確認或重新選擇起始段落。
- 擷取標題後的書目，遇附錄等結束標題停止，不將正文一起送到文獻查詢服務。
- 保留段落編號與完整原文；段落編號不能標成 Word 頁碼。
- 基本模式可將每個非空段落視為一筆，但必須提示跨段文獻的限制；不要宣稱已完美辨識所有文獻邊界。
- 提供逐篇顯示、複製全部書目及下載 TXT。

### 5.2 自動與人工來源

- 自動查詢來源：**Crossref、OpenAlex、PubMed**。
- 有 DOI 時優先查 DOI；沒有 DOI 時使用書目文字搜尋。
- 保存實際查詢時間、方法、來源網址、狀態與候選資料，不把候選當成已核實正確。
- 來源欄位盡可能保留作者、結構化姓名、題名、年份、期刊、卷、期、頁碼、文章編號、DOI、文獻類型及撤稿／更新資訊。
- OpenAlex 可填選用資料庫金鑰，清楚說明與 AI 金鑰不同；只傳至 OpenAlex 官方端點，不寫入報告或查詢網址。
- 依官方速率限制查詢；個別來源失敗不應阻止其他來源，取消操作必須有效。
- 人工來源：**華藝、臺灣博碩士論文知識加值系統、Web of Science、Scopus、Google Scholar、RefCheck**。
- 人工來源提供官方入口、查詢文字複製及實際結果登錄；不假裝已登入、已搜尋或已讀取全文。
- 人工登錄包含來源、實際官方網址、結果、核對依據及時間，明確標示為使用者登錄、非系統獨立驗證。
- 網址只接受適當 HTTP(S) 官方網域，不接受帳密、金鑰參數、可執行協定或冒牌網域。

### 5.3 每篇的狀態

每篇固定給予 R001、R002 等編號，保存完整原文、來源與判定依據，支援篩選：

- **OK（基本書目）**：至少完整題名、第一作者、年份及原文有列出的 DOI 相符；不能僅因 API 找到結果就標 OK。
- **有疑點**：例如 DOI 對應的題名／年份不一致、撤稿標記或人工登錄發現衝突。
- **待核對**：僅找到候選，無法完整確認。
- **查無紀錄**：所選來源正常查詢但未找到；不能推論為虛假文獻。
- **查詢未完成**：連線、權限、額度、解析、取消或尚未查詢。
- **人工確認**：與自動驗證分開標示。

書目存在不代表文內引用主張正確；不同資料庫可能使用同一出版紀錄，不能當成多份獨立證據。

### 5.4 引用格式與修訂建議

- 基本功能至少支援 **APA 7、IEEE 的期刊論文規則**，不需 AI。
- 格式檢查與書目存在性分開呈現：書目 OK 不等於格式正確。
- 檢查適用的日期表示、作者姓氏與縮寫、DOI 表示、期刊、卷期、頁碼、文章編號、暫用卷次與 IEEE 編號結構。
- 先確認文獻類型及來源對應；書籍、學位論文、會議、網頁或無法可靠辨識類型者，不直接套期刊格式。
- 有可靠且完整的來源資料時，**直接列出修正後的完整書目建議**，不能只列「有問題」「應核對」或欄位清單。
- 無法確認的作者、年份、題名、頁碼或 DOI 不得補造。缺必要欄位者移至「需補資料」，不將帶佔位字的內容冒充完成稿。
- 題名大小寫、專有名詞、出版版本等無法確定時明示限制，不使用簡單大小寫轉換破壞名稱。
- 建議稿中的期刊與卷號等依所選樣式呈現適當斜體；純文字抽取無法證明原 Word 已有正確斜體或懸掛縮排，不能聲稱已驗證。
- 詳細區保留完整 **Original／Revised**。新增、調整與待補欄位以紅色粗體／底線標示，保留文字維持原色；原文需調整的片段也可標紅。
- 差異比較必須保留全部文字、逸出 HTML，並限制超長內容的運算成本，不裁切或悄悄省略內容。
- 建議僅存在畫面與報告，不自動新增、刪除或改寫 Word 的參考文獻。

### 5.5 統計規則

- 本機檢查 p 值範圍、負標準差、信賴區間上下界與可辨識的部分檢定值一致性。
- 可用文中統計量作診斷比較，但必須說明 t 的尾端假設及 F／卡方等計算前提。
- **只查錯，不更改統計數值、不提供可直接替換的研究結果，不改動結論。**
- 無原始資料時不能聲稱驗證了分析流程、假設或資料真實性；圖片內公式與未抽取資料不冒充已檢查。
- 只選統計規則時不傳送資料到外站，也不要求 AI 金鑰或不必要的資料傳送同意。

## 六、RefCheck 外部工具

- 清楚提供 https://ref-check.org/ 及官方操作手冊／隱私政策連結。
- 提供：確認擷取範圍 → 複製／下載 TXT → 使用者自行到 RefCheck 檢查 → 匯回 TXT／CSV → 保存外部紀錄。
- 未取得正式公開 API 與使用權限前，不實作或宣稱能自動提交、輪詢、抓回結果，也不繞過登入或使用未公開端點。
- 外部 TXT／CSV 上限 1 MB、10 萬字元；原樣、安全地呈現，記錄來源與匯入時間。
- 不自動把匯入文字匹配為文獻通過，不執行其 HTML／指令，不傳給 AI；可附在重新下載的 HTML／PDF 報告。
- 換文件時清除外部紀錄，非同步匯入也要避免把舊結果放進新文件。

## 七、選用 AI 進階分析

### 7.1 供應商與資料

- 支援 **OpenAI、Google Gemini、xAI Grok**，由使用者選擇並輸入自己的 API Key。
- 供應商各自使用固定官方端點，不自動把失敗請求改送另一家；Grok 不等於 Groq。
- OpenAI 使用 Responses；Gemini 使用 Developer API 的 generateContent；Grok 使用其 Chat Completions 介面。開發時依官方最新文件確認模型與 JSON 格式支援。
- 模型名稱可調整，不把曾存在的名稱當成目前每個帳戶都可用的保證。
- AI 金鑰僅短暫存在記憶體，開始分析後清空輸入框；切換供應商、清除、關閉或離開頁面時清理並取消相關請求。
- 金鑰不得放進 URL、localStorage、sessionStorage、GitHub、日誌或報告；資料庫不得收到 AI 金鑰。
- 分析前說明傳送內容、服務與可能費用，經使用者勾選同意後才送出。

### 7.2 模組與輸出邊界

可勾選學校規範分析、引用格式判讀、文內引用與來源語意、統計敘述疑點。AI 引用樣式可選 APA 7、Harvard、IEEE、MLA 9、Chicago author-date。

- 只傳選取模組所需內容，長文分批，不把全文一次送出後假裝沒有截斷。
- 沿用已完成基本查證的快照，不讓啟用 AI 成為資料庫查證或統計規則的必要條件。
- 要求繁體中文結構化 JSON；欄位限定摘要、問題與排版規則，驗證類別、嚴重度、字串型別與允許值。
- 問題包含位置、原文證據、說明、人工處理方式與判讀來源。
- 不接受 AI 回傳可寫入論文的改寫文字、替換數值或文獻 edits。未知欄位忽略，不能接到文件寫入流程。
- AI 排版規則必須有能在規範原文核對的證據、落在允許清單與合理數值範圍內；填入工作台後仍需預覽與確認。
- 顯示完成批次、失敗批次、取消、時間與 API 回傳用量。部分結果可下載，但不得標為完整成功。
- 處理 400／401／403／404／429、服務錯誤、逾時、截斷、拒答、無效 JSON 與網路錯誤；保留可理解的失敗原因。
- 不直接回顯可能包含金鑰或論文的服務錯誤本文。
- 初始限制：論文 30 萬字元、規範 6 萬字元、200 個書目段落；超出時清楚提示分章處理，不靜默截斷。

### 7.3 可用模型與記憶

- Gemini 可透過官方 models.list 讀取所有分頁，去重並提供下拉選單；選取後填入模型名稱。
- 支援讀取模型資訊的檢查，但「模型列出／可讀取」只代表中繼資料可用，不代表付費生成或結構化輸出已通過。
- 提供明確的「測試此模型」按鈕。經費用同意後，以不含論文的最小請求測試，只有實際成功解析結構化輸出才標記已驗證。
- 成功完成正式 AI 分析也可將模型記為已驗證；不要自動輪流付費測試全部模型。
- 以供應商與金鑰的安全雜湊識別區分紀錄，僅保存模型 ID、狀態及日期，不保存原始金鑰。
- 重新輸入相同金鑰後，恢復此瀏覽器記住的已驗證模型；不同金鑰不能共用驗證結果。
- 明確不存在、不支援所需生成方法／輸出設定、或明確模型權限拒絕者從可選清單排除。
- 401 金鑰問題、429 額度／限流、暫時服務或網路錯誤不能直接推論模型不可用，不因此永久排除。
- 提供清除紀錄及重新檢核；已驗證不保證未來額度與權限不變。儲存受限時不能假裝已持久記住。

## 八、報告：先看修正結果，再看依據

報告不可以再以大篇判定說明、OK 卡片或截斷原文作為主要首頁。

### 8.1 預設「修正結果」

- 直接逐篇列出 **修正後的完整參考文獻建議稿**，附 R 編號與所用格式，能清楚閱讀及選取文字。
- 只將來源足夠、必要欄位完整的建議列在此區；紅色粗體標示實際變動。
- 缺資料者分到「需補資料」，顯示原始文獻與具體缺少項目，不拿通用範本或佔位字當成完成結果。
- 用簡短一句說明建議須人工確認、尚未套用，不在每篇重複長篇免責文字。

### 8.2 分區導覽

依本次內容提供：**修正結果、需補資料、參考文獻對照、內容與統計檢核、排版建議、AI 摘要、外部紀錄、範圍與責任**。

- 詳細對照可選單篇／單項，避免為找一篇文獻翻整份長報告。
- 參考文獻詳細區有 Original／Revised、問題、來源、查詢時間與人工登錄。
- 內容檢核以「論文內容 vs. 建議處理方式」呈現。統計的右側只能列人工核對方式，不提供替換值。
- 未執行、查無結果、外部失敗與未完成要區分，不能用沒有問題數量作為品質保證。
- 不重複列出所有從未使用的人工資料庫空白狀態。

### 8.3 開啟與下載

- 提供「開啟分區網頁報告」、下載獨立 HTML、下載 PDF。
- HTML 在離線開啟時仍能切換分區與項目，使用安全逸出及固定互動程式，不執行使用者內容。
- 網站內預覽用適當隔離的 iframe／對話框，保留可關閉操作。
- PDF 以 A4 分頁、中文可讀、有頁碼。匯出全部分區與全部項目，不因目前網頁只顯示一篇而漏掉其他內容。
- 長段落可分成標示「續」的區塊，不裁切、不漏字、不把標題留在頁尾而內容跑到下頁。
- 若 PDF 採圖像頁面，清楚說明文字搜尋／複製可使用 HTML。

## 九、研究倫理、資料安全與免責聲明

以下是程式邊界，不只是一段提示詞：

- 不自動改寫論文正文、參考文獻或研究結論；不改統計值；只有確認後的允許排版設定能寫入新 Word。
- 不編造文獻、作者、DOI、頁碼、查詢結果或查閱全文紀錄。
- 未取得全文時不保證引用忠實性；未取得原始資料時不保證統計正確性。
- 所有上傳與外部來源均是不可信資料；防止 HTML／腳本注入、文件內提示注入與跨文件結果錯配。
- 依 COPE／UNESCO 的作者責任、人類主導、透明揭露與資料保護原則設計；不宣稱獲得認證或自動符合所有學校／期刊規範。
- 顯示作者與以下免責聲明：

> 本網站僅供研究與教學使用。網站提供的格式整理、AI 分析、文獻查證、統計檢查及報告，均為輔助資訊，不保證其正確性、完整性、即時性或符合各校最新規範。使用者應自行核對原始文獻、研究資料、統計結果、引用內容及學校正式規定，並對最終提交或使用的內容負責。查無文獻不等於虛假，查得書目也不代表引用主張正確。本網站及其報告不代表學校官方審查、認證或學術不端判定。使用者應保留原始檔案，確認有權上傳或傳送資料；AI 使用及模型測試可能產生費用，須由使用者自行負擔。

## 十、圖解與文件

- 交付 README、可直接重用的本開發 Prompt、功能／限制說明與可重現測試指令。
- 提供兩張繁體中文圖片：**系統功能圖、分析流程圖**，用深綠主題與色塊區分基本功能、選用 AI、輸出與倫理邊界。
- 流程圖需清楚畫出免 AI 路徑；Word 排版是另一路徑，需要規範與預覽確認，不把引用修訂建議接到 Word 內容改寫。
- 如使用者指定 image2，僅在工具支援且可核實時宣稱使用該版本；不可暗中替換後仍聲稱符合指定模型。
- 圖片放入儲存庫，README 可預覽，保留完整繪圖 Prompt 與生成方式；不可僅引用開發者電腦上的圖片路徑。

## 十一、測試與驗收

完成下列檢查後才能稱為交付完成：

1. 無任何 AI Key，也能完成 Word 排版、文獻擷取、基本查證、APA 7／IEEE 規則檢查、統計規則與 HTML／PDF 下載。
2. 不需學校規範即可做文獻檢核；同一入口可分別選文獻或統計；僅統計不發送外部請求。
3. Word 修改前有清楚預覽；變更設定後舊預覽失效；產出檔仍可開啟，正文、表格、公式、圖片與前置內容依範圍保留。
4. 測試正確書目、DOI／題名／年份衝突、候選但不確定、查無紀錄、API 失敗及取消，不能將失敗當作查無或正確。
5. 測試日期、暫用卷次、缺卷期頁碼、文章編號、作者列法與非期刊類型；不補造未知欄位。
6. 報告預設頁確實顯示完整修訂建議，缺資料另列；Original／Revised 沒有截斷原文，紅字標記與差異吻合。
7. PDF 包含所有分區與項目；用實際產出的頁面圖片檢查中文字、換頁、頁尾、長書目與是否裁切。
8. 模擬 OpenAI／Gemini／Grok 正常回應、401／403／404／429、截斷、拒答、錯誤 JSON 與逾時；金鑰只出現在正確端點的授權標頭。
9. 模型清單完整分頁；同金鑰恢復記憶、不同金鑰隔離；明確模型失敗被排除，429 不誤刪；重新檢核與清除正常。
10. 測試模型回傳被禁止的 edits 欄位，確認不被使用，也不存在文獻或統計數值寫入路徑。
11. RefCheck 僅匯出／匯入，不自動外送；外部文字安全逸出，更換文件時清除舊結果。
12. 檢查規範選單保存、重複匯入、取消、損壞檔案、超限檔案與跨文件狀態清理。
13. 在約 1366×768 桌面與約 390 px 手機寬度檢查畫面、操作按鈕、分頁與報告入口。
14. 不將模擬 API 測試說成使用者實際金鑰或付費模型已驗證；交付時分清楚實測、模擬與尚未驗證。

## 十二、發布與最終交付

- 指定儲存庫：**profchwu/thesis**。
- GitHub Pages 目標：**https://profchwu.github.io/thesis/**，以 `main` 分支根目錄部署。
- 若已有儲存庫，保留現有歷史與未提交工作，不強制推送、不覆蓋他人變更，不提交金鑰或使用者論文。
- 有已授權的發布權限時，完成測試後提交並推送，再確認正式網址與新版資源可讀取；沒有權限時交付完整程式與具體發布步驟，明確說明阻礙。
- 最終提供網站／儲存庫連結、已實作功能、測試結果及仍須人工確認的限制。不能把原型、假串接或未執行功能當成完成品。

請從檢查現有專案或建立專案開始，完成上述系統。只有缺少必需資料、授權或外部條件時才提出具體問題；一般可逆實作選擇自行採合理方案並繼續。


## 十三、內文引用正確性：基本與進階分層

請實作以下兩層並保留既有書目查證功能：

1. **基本／免 AI**：獨立勾選、本機執行常見中英文作者年份與明示編號對應。列出原句、引用、對應書目及段落位置；標示未找到、年份含 a／b 不一致、多筆候選。只表示書目對應，不能當作語意正確。未辨識、特殊格式及圖片／註腳須明示範圍限制。
2. **進階／使用自己的 AI Key**：針對每個已辨識的句子與引用文獻，傳送必要前後文及實際取得的來源摘要或段落。可重用基本查證取得且書目相符的摘要，也提供逐篇貼上合法來源段落、證據類型與 HTTPS 網址的介面。來源僅存本次記憶體，換文件清除；重新提供來源須重新確認傳送同意。
3. 必須分清楚支持、部分支持、僅主題相關、與來源矛盾、證據不足。審閱因果與相關、研究對象、效果方向／幅度、適用範圍及過度推論；不可憑題名、模型記憶或書目存在就判定支持。
4. 報告逐筆呈現 Original 原句、對應文獻、摘要／全文相關段落的範圍、來源網址、可逐字核對的證據、結論和人工處理建議；疑點用紅色粗體。不生成原句替換、不修改引用或統計數值。
5. AI 結果以引用 ID 與證據 ID 對應；伺服器回傳之外，前端必須驗證引文在提供來源中確實存在。無有效證據、AI 遺漏、重複 ID 或未完成項目，均降為無法判定並列入報告，不默默省略。不把逐字驗證誤稱為語意正確保證。
6. 沒有來源證據的項目不耗用 AI 請求，直接列為待查證；只有摘要則明示僅依摘要。使用者貼上的來源關聯與完整性未經獨立驗證。不自動下載受限全文、不聲稱讀過未提供的全文。
7. 網頁／HTML／PDF 分成基本引用與進階主張區；網頁逐筆切換，PDF 必須包含所有項目與證據。報告只是檢核建議，原始 Word 的文字與數值須完全保留。
8. 測試作者年份、中文、複數引用、編號、缺書目、年份錯配、重複書目、無證據、偽造引文、錯誤 AI 輸出、取消、不同文件、下載報告及原始 Word 未修改。付費模型僅能在取得真實金鑰與用量授權後實測；否則明確標示為模擬。
